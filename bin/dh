#!/usr/bin/env ruby

require 'rubygems'

$LOAD_PATH.unshift(File.dirname(__FILE__) + '/../lib')

require 'dreamhost'
require 'dreamhost/command'

args = ARGV.dup
ARGV.clear
command = args.shift.strip rescue 'help'

DreamHost::Command.run(command, args)